export type InboxMode = "global" | "subproject" | "unassigned";

export type AreaOption = {
  id: string;
  name: string;
};

export type ProjectOption = {
  id: string;
  name: string;
};

export type SubProjectOption = {
  id: string;
  name: string;
};
